// lib/utils/file_picker_stub.dart
// Non-web stub implementation — returns null (no file selected).
import 'dart:typed_data';

Future<Uint8List?> pickImageFileWeb() async {
  // On non-web platforms this stub is used (via conditional import).
  // You can expand this to use platform-specific pickers (image_picker) if desired.
  return null;
}
